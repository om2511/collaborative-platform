# Test Environment Configuration
NODE_ENV=test
PORT=5002

# Database
MONGODB_URI=mongodb://localhost:27017/collab_test
TEST_MONGODB_URI=mongodb://localhost:27017/collab_test

# JWT Secret (Test only)
JWT_SECRET=test-jwt-secret-key-for-testing-only-do-not-use-in-production

# AI Service (Mock in tests)
GEMINI_API_KEY=test-key-mocked-in-tests

# Rate Limiting (Disabled in tests)
RATE_LIMIT_ENABLED=false

# CORS (Permissive for tests)
CORS_ORIGIN=*

# Logging
LOG_LEVEL=error

# File Upload
MAX_FILE_SIZE=5MB
UPLOAD_PATH=./uploads/test